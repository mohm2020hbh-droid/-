extends "res://tests/integration/test_world_01_playthrough.gd"
## The World 01 playthrough tests, on World 02: every route finishes, the
## run is deterministic, respawns keep the timing.


func world_index() -> int:
	return 1
